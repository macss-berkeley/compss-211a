import {skills} from './catalog.mjs';
const card=(id,skill,deck,prompt,code,answer,answerCode='',explanation='')=>({id,skill,deck,prompt,code,answer,answerCode,explanation});
export const flashcards=[
card("assignment-copy", "variables-and-assignment", "Python", "What value does print(share) display? Reorder the lines so share uses the corrected respondent count of 80.", "respondents = 100\nbus_riders = 20\nshare = bus_riders / respondents\nrespondents = 80\nprint(share)", "It prints 0.2. Move the share calculation below respondents = 80 and before print(share) to get 0.25.", "share = bus_riders / respondents", "Assignment stores the calculated value; it does not create a formula that updates when another variable changes."),
card("type-conversion", "basic-data-types", "Python", "The counts came from a CSV. Fix the calculation so total is 35.", "counts = [\"12\", \"8\", \"15\"]\ntotal = sum(counts)", "Convert each count to a number before adding.", "total = sum(int(value) for value in counts)", "sum() cannot add these strings to its initial integer total."),
card("list-index", "lists-and-indexing", "Python", "Write code that assigns the last item in counts to latest, or None if the list is empty.", "counts = [120, 135, 128]", "", "latest = counts[-1] if counts else None", "Indexing an empty list raises IndexError."),
card("list-slice", "lists-and-indexing", "Python", "In rows, the first item is a header and the last is a total. Write a slice that selects only the three observation values.", "rows = [\"requests\", 12, 8, 15, \"total: 35\"]", "", "observations = rows[1:-1]", "The result is [12, 8, 15]. The stop position is excluded."),
card("dict-key", "dictionaries-and-keys", "Python", "Write an expression that divides the \"responded\" count by the \"invited\" count in record to calculate the response rate.", "record = {\"invited\": 250, \"responded\": 175}", "", "rate = record[\"responded\"] / record[\"invited\"]", "The response rate is 0.7. Use the invited count as the denominator."),
card("string-clean", "strings", "Python", "Write code that counts bus responses despite differences in case and surrounding spaces.", "modes = [\" Bus\", \"BUS \", \"bike\", \"bus\"]", "", "bus_count = sum(mode.strip().lower() == \"bus\" for mode in modes)", "The count is 3. These values are all strings."),
card("compare-assign", "operators", "Python", "Fix the if condition so open_count increases only when status is \"open\". Assume open_count is already initialized.", "status = \"closed\"\nif status = \"open\":\n    open_count += 1", "Use == to compare status with \"open\". Assume open_count has already been initialized.", "if status == \"open\":\n    open_count += 1", "A single = is assignment and is invalid in this condition."),
card("remainder", "operators", "Python", "Use the remainder after division by 5 to assign respondent_id to a group labeled 0–4. Which group does ID 17 enter?", "respondent_id = 17", "", "group = respondent_id % 5", "ID 17 goes to group 2. This is deterministic grouping, not random assignment."),
card("conditional-boundary", "conditionals", "Python", "Repair this rule: ages 18–64 are eligible. Both endpoints count.", "age = 18\nif 18 < age < 64:\n    eligible = True\nelse:\n    eligible = False", "", "eligible = 18 <= age <= 64", "The original condition excludes both 18 and 64."),
card("loop-total", "for-loops", "Python", "Write code that sums the costs and counts the nonmissing entries. Exclude None; include zero in the count.", "costs = [0, 12, None, 8]", "", "total = 0\nobserved = 0\nfor cost in costs:\n    if cost is not None:\n        total += cost\n        observed += 1", "The sum is 20 and the observed count is 3. A truthiness check such as if cost would also exclude zero."),
card("call-default", "calling-functions", "Python", "Call per_trip with cost=24 and trips=3. What value of trips is used if you call per_trip(24) instead?", "def per_trip(cost, trips=1):\n    return cost / trips", "", "per_trip(24, trips=3)", "This returns 8.0. per_trip(24) uses the default of one trip and returns 24.0."),
card("return-print", "writing-functions", "Python", "What value is assigned to result when result = double(3) runs?", "def double(n):\n    print(n * 2)\n\nresult = double(3)", "None", "", "The function prints 6, but has no return statement."),
card("write-function", "writing-functions", "Python", "Write response_rate(responded, invited) to divide responded by invited. Return None if invited is zero. Assume nonnegative counts.", "", "", "def response_rate(responded, invited):\n    if invited == 0:\n        return None\n    return responded / invited", "A zero denominator gives no defined response rate, rather than a rate of zero."),
card("method-call", "values-variables-functions-and-methods", "Python", "Repair the length calculation so it counts the characters in text after removing surrounding spaces.", "text = \"  bus  \"\nlength = text.strip().len()", "", "length = len(text.strip())", "The result is 3. strip() is a string method; len() is called as a function."),
card("trace-loop", "read-code-line-by-line", "Python", "What list does print(kept) display after the loop finishes?", "kept = []\nfor n in [2, 5, 8]:\n    if n > 4:\n        kept.append(n * 2)\nprint(kept)", "[10, 16]", "", ""),
card("import-alias", "import-packages", "Python", "Change the import so pd.read_csv(\"requests.csv\") can run. Assume pandas is installed and the file exists.", "import pandas\ndf = pd.read_csv(\"requests.csv\")", "", "import pandas as pd\ndf = pd.read_csv(\"requests.csv\")", "Importing pandas alone binds the name pandas, not pd."),
card("keyerror", "read-tracebacks", "Python", "The expression df[\"seat\"] raises this KeyError. What should you inspect in df?", "df[\"seat\"]\n# KeyError: 'seat'", "Check whether the column \"seat\" exists, including spelling and capitalization.", "", "Inspect df.columns."),
card("assert-count", "test-your-code", "Python", "After a left join with a lookup, assert that the request row count has not changed.", "before = len(requests)\njoined = requests.merge(districts, on=\"district_id\", how=\"left\")", "", "assert len(joined) == before", "A failure may indicate duplicate lookup keys. Also use validate=\"many_to_one\" when each district ID should identify one lookup row."),
card("module-call", "import-your-own-code", "Python", "After import case_summary, write a call to its summarize_requests function with requests as the argument.", "import case_summary", "", "case_summary.summarize_requests(requests)", ""),
card("load-csv", "load-tabular-data", "pandas", "Read commute.csv into df. The file uses semicolons between columns and \"not reported\" for missing values. Assume pandas is imported as pd.", "File: commute.csv\nmode;days\nbus;3\nbike;not reported", "", "df = pd.read_csv(\"commute.csv\", sep=\";\", na_values=[\"not reported\"])", "The separator and missing-value code affect how the data are interpreted."),
card("shape", "inspect-a-dataframe", "pandas", "The left table has one row per respondent. What changed after the join, and what should you check in the lookup table?", "respondents.shape  # (120, 5)\njoined.shape       # (150, 7)", "The join added 30 rows as well as two columns. Check for duplicate join keys in the lookup.", "lookup[lookup[\"id\"].duplicated(keep=False)]", "Multiple lookup matches can repeat respondent rows and distort later counts."),
card("select-columns", "select-and-filter", "pandas", "Write an expression that selects the \"mode\" and \"days\" columns from df as a DataFrame.", "", "", "df[[\"mode\", \"days\"]]", ""),
card("filter-rows", "select-and-filter", "pandas", "Select the rows from df where \"days\" is at least 3.", "", "", "df[df[\"days\"] >= 3]", ""),
card("combine-pandas", "combining-conditions", "pandas", "Select rows from df where \"days\" is at least 3 and \"mode\" is \"bus\".", "", "", "df[(df[\"days\"] >= 3) & (df[\"mode\"] == \"bus\")]", "Put parentheses around each comparison. Use & for elementwise AND."),
card("new-column", "create-or-modify-columns", "pandas", "Create df[\"total\"] by multiplying \"fare\" by \"trips\" for each row.", "", "", "df[\"total\"] = df[\"fare\"] * df[\"trips\"]", ""),
card("missing-mean", "missing-values", "pandas", "Calculate the mean of the nonmissing costs. Then calculate the mean if each None is replaced with zero. Why do the results differ?", "costs = [10, None, 30, None]", "Observed mean: 20. After filling with zero: 10. The second result assumes both missing costs were zero.", "", "Report missingness; use an imputation rule only when you can justify it."),
card("count-size", "summaries-and-grouping", "pandas", "After grouping these rows by district, what does size() return for each group, and what does count() return for its cost column?", "district  cost\nA         10\nA         missing\nB         0", "District A: size 2, cost count 1. District B: size 1, cost count 1.", "df.groupby(\"district\").size()\ndf.groupby(\"district\")[\"cost\"].count()", "Missing costs still occupy rows. Zero is an observed cost."),
card("category-shares", "sort-and-count", "pandas", "Write an expression that calculates the proportion of each mode among nonmissing values in df[\"mode\"].", "", "", "df[\"mode\"].value_counts(normalize=True)", ""),
card("preserve-id", "convert-data-types", "pandas", "A join uses \"00123\" in one table and the integer 123 in another. Why is converting both to integers risky, and what should you check?", "", "Conversion discards leading zeros and may collapse distinct identifiers. Check the identifier specification and reload the original IDs as text if possible.", "", "Only add padding when the source specification defines a fixed width; lost zeros cannot always be reconstructed."),
card('join-duplicates','join-tables','pandas','How many rows will the inner join produce?','left IDs:  A, B\nright IDs: A, A, B\nleft.merge(right, on="id", how="inner")','3','','A produces two matching pairs; B produces one.'),
card("nested-access", "csv-and-json", "Python", "Write an expression that retrieves \"title\" from the first item in response[\"results\"].", "response = {\"results\": [{\"title\": \"Transit\"}]}", "", "response[\"results\"][0][\"title\"]", ""),
card("month-year", "dates-and-times", "pandas", "From the datetime column df[\"date\"], create a grouping key that keeps each year-and-month combination separate.", "", "", "df[\"date\"].dt.to_period(\"M\")", "Using .dt.month alone combines the same month across different years."),
card("optional-key", "dictionaries-and-keys", "Python", "Write an expression that retrieves \"country\" from the dictionary record, returning None if that key is absent.", "", "", "record.get(\"country\")", "Bracket access raises KeyError when the key is absent."),
card("list-alias", "lists-and-indexing", "Python", "What are the items in original after columns.append(\"program\") runs?", "original = [\"id\", \"cost\"]\ncolumns = original\ncolumns.append(\"program\")", "[\"id\", \"cost\", \"program\"]", "", "Both names refer to the same list. Use original.copy() if you need a separate list."),
card("format-percentage", "strings", "Python", "Write an f-string that displays share as a percentage with one decimal place.", "share = 0.237", "", "f\"{share:.1%}\"", "The result is \"23.7%\". The format multiplies by 100 for display."),
card("membership-values", "operators", "Python", "Write a Python expression that is True when mode is \"bus\" or \"train\", and False otherwise.", "", "", "mode in [\"bus\", \"train\"]", ""),
card("function-missing", "writing-functions", "Python", "Write cost_label(cost): return \"missing\" for None, \"low\" below 20, otherwise \"high\".", "", "", "def cost_label(cost):\n    if cost is None:\n        return \"missing\"\n    return \"low\" if cost < 20 else \"high\"", ""),
card("name-error", "read-tracebacks", "Python", "What should you check after NameError: name 'df' is not defined?", "", "Check whether df was assigned, whether its name is spelled correctly, and whether the cell or script that creates it has run.", "", ""),
card("notebook-hidden-state", "jupyter-notebooks", "Python", "A notebook works only after you run some cells out of order. How would you check that it runs correctly from a fresh kernel?", "", "Restart the kernel and run all cells from top to bottom. Fix dependencies on variables created in an earlier session.", "", ""),
card("api-status", "web-apis", "Python", "After requests.get() returns, add a check for an HTTP error, then parse the response as JSON.", "response = requests.get(url, timeout=30)", "", "response.raise_for_status()\ndata = response.json()", "A successful status still does not guarantee the fields or data are what you expected."),
card("api-next-page", "web-apis", "Python", "An API returns 25 records and a next-page cursor. Can you report that the query found 25 records?", "", "No. Follow the pagination instructions until there is no next page, then check for repeated IDs before reporting a total.", "", ""),
card("api-secret-env", "keep-api-keys-safe", "Python", "Read API_KEY from environment variables without putting its value in the notebook.", "", "", "import os\napi_key = os.environ[\"API_KEY\"]", "Set the variable outside the notebook. Do not print it or commit a .env file."),
card("inspect-dtypes", "inspect-a-dataframe", "pandas", "Write an expression that shows the data type of each column in df.", "", "", "df.dtypes", ""),
card("missing-counts", "missing-values", "pandas", "Write an expression that counts missing values in each column of df.", "", "", "df.isna().sum()", ""),
card("missing-share", "missing-values", "pandas", "Write an expression that calculates the fraction of rows where df[\"cost\"] is missing.", "", "", "df[\"cost\"].isna().mean()", "True is treated as 1 and False as 0. Multiply by 100 to report a percentage."),
card("blank-is-not-na", "missing-values", "pandas", "How many missing values does s.isna().sum() count? Replace empty or whitespace-only strings with missing values, then count again.", "s = pd.Series([\"\", \"   \", None, \"bus\"])", "Initially 1. After normalizing blanks, 3.", "clean = s.replace(r\"^\\s*$\", pd.NA, regex=True)\nclean.isna().sum()", "Empty strings and strings containing spaces are not automatically treated as missing in an existing Series."),
card("normalize-blanks", "clean-and-document", "pandas", "Replace empty or whitespace-only strings in df[\"district\"] with a missing value.", "", "", "df[\"district\"] = df[\"district\"].replace(r\"^\\s*$\", pd.NA, regex=True)", ""),
card("standardize-labels", "clean-and-document", "pandas", "Remove surrounding spaces and convert text to lowercase in df[\"mode\"].", "", "", "df[\"mode\"] = df[\"mode\"].str.strip().str.lower()", "Check the resulting labels before combining categories that may mean different things."),
card("map-labels", "clean-and-document", "pandas", "In df[\"frequency\"], replace \"M\" with \"monthly\" and \"W\" with \"weekly\". Leave other values unchanged.", "", "", "df[\"frequency\"] = df[\"frequency\"].replace({\"M\": \"monthly\", \"W\": \"weekly\"})", ""),
card("numeric-coerce", "convert-data-types", "pandas", "Convert df[\"cost\"] from text to numbers, treating values that cannot be converted as missing.", "", "", "df[\"cost\"] = pd.to_numeric(df[\"cost\"], errors=\"coerce\")", "Count and inspect the values that became missing; conversion does not explain why they were invalid."),
card("numeric-failure-audit", "convert-data-types", "pandas", "Select the original entries in df[\"cost\"] that were nonmissing but became missing after this numeric conversion.", "numeric_cost = pd.to_numeric(df[\"cost\"], errors=\"coerce\")", "", "df.loc[df[\"cost\"].notna() & numeric_cost.isna(), \"cost\"]", ""),
card("read-id-text", "load-tabular-data", "pandas", "Load respondents.csv while preserving leading zeros in respondent_id.", "", "", "df = pd.read_csv(\"respondents.csv\", dtype={\"respondent_id\": \"string\"})", ""),
card("drop-missing-subset", "missing-values", "pandas", "Select rows from df where \"cost\" is nonmissing, even if other columns contain missing values.", "", "", "df.dropna(subset=[\"cost\"])", ""),
card("zero-is-observed", "missing-values", "pandas", "Why should missing income usually not be replaced with 0?", "", "Zero records an observed amount. Missing means the amount is unknown. Replacing it changes summaries and may bias comparisons.", "", ""),
card("duplicate-key-count", "explore-a-new-dataset", "pandas", "Count repeated values in df[\"respondent_id\"] after their first occurrence.", "", "", "df[\"respondent_id\"].duplicated().sum()", ""),
card("inspect-all-duplicates", "explore-a-new-dataset", "pandas", "Select every row in df whose \"case_id\" occurs more than once, including the first occurrence.", "", "", "df[df[\"case_id\"].duplicated(keep=False)]", ""),
card("deduplicate-exact", "clean-and-document", "pandas", "Return a copy of df with duplicate rows removed, keeping one occurrence of each distinct row.", "", "", "df.drop_duplicates()", "First confirm that repeated identical records are accidental; repeated events may be valid observations."),
card("range-audit", "clean-and-document", "pandas", "Select rows from df where \"age\" is nonmissing and is below 0 or above 120.", "", "", "df[df[\"age\"].notna() & ~df[\"age\"].between(0, 120)]", "Inspect the source before treating an unusual value as an error."),
card("filter-membership", "select-and-filter", "pandas", "Select rows from df where \"mode\" is \"bus\" or \"train\".", "", "", "df[df[\"mode\"].isin([\"bus\", \"train\"])]", ""),
card("filter-exclusion", "select-and-filter", "pandas", "Select rows from df where \"status\" is neither \"test\" nor \"duplicate\".", "", "", "df[~df[\"status\"].isin([\"test\", \"duplicate\"])]", ""),
card("filter-either", "combining-conditions", "pandas", "Select rows from df where \"cost\" is greater than 100 or \"days\" is greater than 4.", "", "", "df[(df[\"cost\"] > 100) | (df[\"days\"] > 4)]", ""),
card("loc-row-column", "select-and-filter", "pandas", "Select the \"case_id\" and \"cost\" columns from df, including only rows where \"cost\" is greater than 100.", "", "", "df.loc[df[\"cost\"] > 100, [\"case_id\", \"cost\"]]", ""),
card("loc-update", "create-or-modify-columns", "pandas", "In df, replace negative values in \"cost\" with a missing value.", "", "", "df.loc[df[\"cost\"] < 0, \"cost\"] = pd.NA", "Use one .loc assignment instead of chained indexing such as df[mask][\"cost\"] = ... ."),
card("filter-copy", "modify-existing-code", "pandas", "Create a separate copy of the rows in df where \"mode\" is not \"Remote\".", "", "", "commuters = df[df[\"mode\"] != \"Remote\"].copy()", ""),
card("denominator-zero", "create-or-modify-columns", "pandas", "Create df[\"cost_per_trip\"] by dividing \"cost\" by \"trips\" for each row. Leave the result missing when \"trips\" is zero.", "", "", "df[\"cost_per_trip\"] = df[\"cost\"] / df[\"trips\"].replace(0, float(\"nan\"))", ""),
card("sort-descending", "sort-and-count", "pandas", "Sort the rows in df by \"request_count\", from largest to smallest.", "", "", "df.sort_values(\"request_count\", ascending=False)", ""),
card("counts-include-missing", "sort-and-count", "pandas", "Count the occurrences of each value in df[\"mode\"], including missing values.", "", "", "df[\"mode\"].value_counts(dropna=False)", ""),
card("group-count-rows", "summaries-and-grouping", "pandas", "Count the rows in df for each \"district\", including a group for missing districts. Each row is one request.", "", "", "df.groupby(\"district\", dropna=False).size()", ""),
card("group-median-and-n", "summaries-and-grouping", "pandas", "For each \"program\" in df, calculate the median \"cost\" and the number of nonmissing costs.", "", "", "df.groupby(\"program\").agg(\n    median_cost=(\"cost\", \"median\"),\n    observed_costs=(\"cost\", \"count\")\n)", ""),
card("unique-respondents", "summaries-and-grouping", "pandas", "For each \"program\" in df, count distinct nonmissing values of \"respondent_id\".", "", "", "df.groupby(\"program\")[\"respondent_id\"].nunique()", ""),
card("left-join-lookup", "join-tables", "pandas", "Attach district names to requests while retaining every request. Each lookup district_id should be unique.", "", "", "requests.merge(districts, on=\"district_id\", how=\"left\", validate=\"many_to_one\")", ""),
card("unmatched-join", "join-tables", "pandas", "Find requests whose district_id has no match in the district lookup.", "", "", "joined = requests.merge(districts, on=\"district_id\", how=\"left\", indicator=True, validate=\"many_to_one\")\nunmatched = joined[joined[\"_merge\"] == \"left_only\"]", ""),
card("join-key-types", "join-tables", "pandas", "IDs match visually, but a merge fails or finds no matches. What should you inspect?", "", "Compare key types, leading zeros, spaces, capitalization, and missing values in both tables. Normalize only differences that do not change the meaning of an ID.", "", ""),
card("parse-known-date", "dates-and-times", "pandas", "Convert df[\"date\"] from day/month/year strings to datetimes. Treat invalid dates as missing.", "", "", "df[\"date\"] = pd.to_datetime(df[\"date\"], format=\"%d/%m/%Y\", errors=\"coerce\")", "Specify the known format; \"03/04/2026\" is ambiguous without one."),
card("half-open-date-filter", "dates-and-times", "pandas", "Select rows from df where \"date\" falls in October 2026, including times late on October 31. The column contains datetimes without a time zone.", "", "", "df[(df[\"date\"] >= \"2026-10-01\") & (df[\"date\"] < \"2026-11-01\")]", ""),
card("elapsed-hours", "dates-and-times", "pandas", "For each row in df, calculate the number of hours from \"opened\" to \"closed\". Both columns contain datetimes.", "", "", "(df[\"closed\"] - df[\"opened\"]).dt.total_seconds() / 3600", "Check for negative durations and missing timestamps before summarizing."),
card("date-local-zone", "dates-and-times", "pandas", "Convert the UTC datetimes in df[\"timestamp\"] to Berkeley local time. The column already has a UTC time zone.", "", "", "df[\"timestamp\"].dt.tz_convert(\"America/Los_Angeles\")", "tz_convert changes the displayed zone of an already-aware timestamp. It does not assign a zone to naive times."),
card("literal-text-search", "clean-text", "pandas", "Select rows from df where \"comment\" contains the substring \"bus\", ignoring case. Treat missing comments as nonmatches.", "", "", "df[df[\"comment\"].str.contains(\"bus\", case=False, na=False, regex=False)]", "This matches substrings, including \"business\". A whole-word rule requires a different pattern."),
card("text-length-audit", "clean-text", "pandas", "Calculate the number of characters in each value of df[\"comment\"], keeping missing comments as missing.", "", "", "df[\"comment\"].str.len()", "Compare these lengths with any supplied length field; do not assume that field used the same text or counting rule."),
card("concat-batches", "data-structures", "pandas", "Combine the rows of september and october into one DataFrame with a new integer index. Both DataFrames have the same columns.", "", "", "pd.concat([september, october], ignore_index=True)", "Concatenation stacks rows; it does not match records by ID or remove duplicates."),
card("save-no-index", "other-file-formats", "pandas", "Save df to cleaned.csv without writing the DataFrame index as an extra column.", "", "", "df.to_csv(\"cleaned.csv\", index=False)", ""),
card("excel-header-rows", "messy-spreadsheets", "pandas", "In survey.xlsx, the first two rows contain titles and row 3 contains column names. Read the sheet using row 3 as the header.", "", "", "df = pd.read_excel(\"survey.xlsx\", skiprows=2)", "Inspect the result for footnotes, totals rows, merged cells, and columns read as text."),
card("reconcile-counts", "clean-and-document", "pandas", "A filter leaves 93 of 100 records. What should your cleaning note record?", "", "The rule, why it was used, the 7 removed records, and the 93 remaining records. Keep the raw file and make the change reproducible.", "", ""),
card("check-unique-key", "test-your-code", "pandas", "Assert that case_id is both nonmissing and unique.", "", "", "assert df[\"case_id\"].notna().all()\nassert df[\"case_id\"].is_unique", "Uniqueness alone does not rule out a single missing key."),
card("sql-select-where", "sql-basics", "SQL", "Write a SQL query that selects case_id and district from requests where status is \"open\".", "requests(case_id, district, status)", "", "SELECT case_id, district\nFROM requests\nWHERE status = 'open';", ""),
card("sql-null", "sql-basics", "SQL", "Select requests whose closed_at value is NULL.", "requests(case_id, closed_at)", "", "SELECT * FROM requests WHERE closed_at IS NULL;", "NULL is tested with IS NULL, not = NULL."),
card("sql-count-null", "sql-basics", "SQL", "A table has 100 rows and 20 NULL costs. What do COUNT(*) and COUNT(cost) return?", "", "COUNT(*) returns 100. COUNT(cost) returns 80.", "", ""),
card("sql-group-having", "sql-basics", "SQL", "Write a SQL query that returns each district and its request count, including only districts with at least 10 requests.", "requests(case_id, district)", "", "SELECT district, COUNT(*) AS n\nFROM requests\nGROUP BY district\nHAVING COUNT(*) >= 10;", "WHERE filters rows before grouping; HAVING filters groups."),
card("sql-left-join", "sql-basics", "SQL", "Keep every request and attach its district name, matching on district_id.", "requests(case_id, district_id)\ndistricts(district_id, name)\nThe lookup has one row per district_id.", "", "SELECT r.case_id, d.name\nFROM requests AS r\nLEFT JOIN districts AS d\nON r.district_id = d.district_id;", ""),
card("sql-top-counts", "sql-basics", "SQL", "Return the five categories with the most requests; break count ties alphabetically.", "requests(case_id, category)", "", "SELECT category, COUNT(*) AS n\nFROM requests\nGROUP BY category\nORDER BY n DESC, category ASC\nLIMIT 5;", ""),
card("unit-of-observation", "explore-a-new-dataset", "Analysis", "You have 240 service requests from 180 distinct resident IDs. Write a defensible one-sentence description of these counts.", "", "The dataset contains 240 requests submitted by 180 distinct recorded resident IDs.", "", "This does not count residents who submitted no requests; it also assumes the recorded IDs identify people consistently."),
card("denominator-observed", "summaries-and-grouping", "Analysis", "Of 100 respondents, 20 report taking the bus and 10 have missing mode. What share of respondents with a known mode take the bus?", "", "20 / 90, or about 22.2%.", "", "Report the denominator and the 10 missing responses. A share of all respondents uses a different denominator."),
card("median-outliers", "summaries-and-grouping", "Analysis", "Calculate the mean and median. What would you check before removing the largest value?", "costs = [20, 25, 30, 35, 900]", "Mean: 202. Median: 30. Check the source, units, and reporting period for 900 before deciding whether it is an error.", "", "If 900 is valid, removing it changes the population being described."),
card("weighted-share", "public-data-and-survey-weights", "Analysis", "How do you calculate a survey-weighted proportion for a binary indicator?", "", "Sum weight × indicator, then divide by the sum of weights for respondents with an observed indicator.", "", "Use the appropriate survey weights and the same eligible respondents in numerator and denominator. Complex survey uncertainty requires the survey design."),
card("selection-bias", "public-data-and-survey-weights", "Analysis", "A transit forum poll has 200 replies: 150 favor a new route. Rewrite “75% of city residents support the route” to match the evidence.", "", "Of the 200 forum poll replies, 150 (75%) favored the new route.", "", "The replies do not establish support among all city residents. Participation and forum membership may be selective."),
card("negation-cleaning", "stop-words-and-preprocessing-choices", "Analysis", "If you remove \"not\" and \"very\" as stop words, which tokens remain in each comment? What would you change before sentiment analysis?", "[\"not\", \"reliable\"]\n[\"very\", \"reliable\"]", "Both become [\"reliable\"]. Preserve negation, then inspect how the sentiment method handles it.", "", "Removing \"not\" erases a distinction needed to interpret these comments."),
card("tfidf-meaning", "word-counts-and-tf-idf", "Analysis", "“Transit” has a high TF-IDF weight in both a pro-transit post and an anti-transit post. Can that feature alone separate support from opposition? Explain.", "", "No. The weight measures the term’s prominence relative to the fitted corpus, not the author’s stance. Inspect context and evaluate any stance classifier on labeled examples.", "", "A relevant term can appear in both supportive and critical writing."),
card("cluster-interpretation", "similarity-and-clustering", "Analysis", "What should you inspect before naming a cluster of documents?", "", "Read several documents, including less typical examples. Compare them with documents in other clusters and check whether the label fits.", "", "Cluster membership depends on the text representation and model settings."),
card("accuracy-baseline", "evaluate-predictions", "Analysis", "A classifier is 90% accurate when 90% of examples are negative. What baseline should you compare it with?", "", "Always predicting negative also gives 90% accuracy.", "", "Inspect errors by class to see whether the classifier detects positive cases."),
card("small-cell-privacy", "protect-people-s-privacy", "Analysis", "Why can a table with no names still reveal personal information when a subgroup has one person?", "", "The subgroup label may identify that person, allowing readers to infer their response.", "", "Review combinations of attributes and external information. Aggregate or suppress risky detail under the applicable data-sharing rules.")
];
export const cardIds=new Set(flashcards.map(c=>c.id));
const DAY=86400000;
export function reviewOptions(previous){
  const days=previous?.intervalDays||0;
  return [
    {grade:'again',label:'Again',days:1/1440},
    {grade:'hard',label:'Hard',days:days<1?10/1440:Math.min(365,Math.ceil(days*1.2))},
    {grade:'good',label:'Good',days:days<1?1:Math.min(365,Math.ceil(days*2))},
    {grade:'easy',label:'Easy',days:days<1?14:Math.min(365,Math.max(14,Math.ceil(days*3)))}
  ];
}
export function scheduleReview(previous,grade,now=new Date()){
  const option=reviewOptions(previous).find(o=>o.grade===grade);
  if(!option)throw new Error('Invalid review rating');
  return {intervalDays:grade==='again'?0:option.days,dueAt:new Date(now.getTime()+Math.round(option.days*DAY)).toISOString(),updatedAt:now.toISOString(),reviews:(previous?.reviews||0)+1,grade};
}
export function setCardSuspended(previous,suspended,now=new Date()){
  const record=previous||{intervalDays:0,dueAt:now.toISOString(),reviews:0,grade:'again'};
  return {...record,suspended,updatedAt:now.toISOString(),...(!suspended?{dueAt:now.toISOString()}: {})};
}
export function intervalLabel(days){return days<1?`${Math.round(days*1440)} min`:`${days} ${days===1?'day':'days'}`;}
export function availableCards({week,scope='covered',deck='all',skill=null}){
  return flashcards.filter(c=>(!skill||c.skill===skill)&&(deck==='all'||c.deck===deck)&&(scope==='all'||skills.find(s=>s.id===c.skill).releaseWeek<=week));
}
export function dueCards(cards,reviews,now=new Date()){
  return cards.filter(c=>!reviews[c.id]?.suspended&&(!reviews[c.id]||Date.parse(reviews[c.id].dueAt)<=now.getTime())).sort((a,b)=>{
    const x=reviews[a.id]?.reviews?reviews[a.id]:null,y=reviews[b.id]?.reviews?reviews[b.id]:null;
    return x&&y?Date.parse(x.dueAt)-Date.parse(y.dueAt):x?-1:y?1:0;
  });
}
export function validateCardReviews(raw={}){
  if(!raw||typeof raw!=='object'||Array.isArray(raw))throw new Error('Invalid flashcard backup.');
  const result={};
  for(const [id,r] of Object.entries(raw)){
    if(!cardIds.has(id))continue;
    if(!r||!Number.isFinite(r.intervalDays)||r.intervalDays<0||r.intervalDays>365||!Number.isInteger(r.reviews)||r.reviews<0||r.reviews>1000000||!['again','hard','good','easy'].includes(r.grade)||!Number.isFinite(Date.parse(r.dueAt))||!Number.isFinite(Date.parse(r.updatedAt))||(r.suspended!==undefined&&typeof r.suspended!=='boolean'))throw new Error('Invalid flashcard review.');
    result[id]={intervalDays:r.intervalDays,reviews:r.reviews,grade:r.grade,dueAt:r.dueAt,updatedAt:r.updatedAt,...(r.suspended!==undefined?{suspended:r.suspended}:{})};
  }
  return result;
}
